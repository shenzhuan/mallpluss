<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mei.zhuang.dao.sys.CrmStoreMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.mei.zhuang.entity.sys.CrmStore">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="dept_id" property="deptId" />
        <result column="channel_id" property="channelId" />
        <result column="store_img" property="storeImg" />
        <result column="store_no" property="storeNo" />
        <result column="province" property="province" />
        <result column="city" property="city" />
        <result column="region" property="region" />
        <result column="address" property="address" />
        <result column="longitude" property="longitude" />
        <result column="latitude" property="latitude" />
        <result column="status" property="status" />
        <result column="create_user_id" property="createUserId" />
        <result column="create_date" property="createDate" />
        <result column="create_time" property="createTime" />
        <result column="update_user_id" property="updateUserId" />
        <result column="update_date" property="updateDate" />
        <result column="update_time" property="updateTime" />
        <result column="version" property="version" />
        <result column="telphone" property="telphone" />
    </resultMap>

    <select id="getStoreList" resultType="map">
        SELECT
            id,
            p_id as "pId",
            name,
            false as open,
            false nocheck,
            org_type
        FROM
            crm_sys_dept
    </select>

    <select id="getStoreDict" resultType="com.arvato.common.msg.DictData">
        select
        id as value,name as text
        from
        crm_sys_dept
        <where>
            org_type = '3'
            <if test="param.storeName != null and param.storeName != ''">
                and name like concat('%',#{param.storeName},'%')
            </if>
            <if test="deptIds != null and deptIds.size > 0">
                AND id in
                <foreach collection="deptIds" index="index" item="deptId" separator="," open="(" close=")">
                    #{deptId}
                </foreach>
            </if>
        </where>
        <if test="param.limit != null">
            limit #{param.limit}
        </if>
        <if test="param.offset != null">
            offset #{param.offset}
        </if>
    </select>

</mapper>
